# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:
  - type: script
    snippets:
      - "chmod +x /usr/libexec/sblue-system-setup.sh"
  - type: systemd
    user:
      enabled:
        - sblue-system-setup.service
