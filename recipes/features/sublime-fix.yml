# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
# TODO: check if weaker crypto policies can help
#       https://github.com/rrenomeron/bootc-images/blob/main/recipes/features/vscode.yml
modules:
  - type: script
    snippets:
      - "curl -L \"https://download.sublimetext.com/sublime-merge-2121-1.x86_64.rpm\" > sublime-merge.rpm"
      - "rpm -iv --nodigest --nofiledigest --force sublime-merge.rpm"
      - "rm sublime-merge.rpm"
