modules:
  # TODO: check guide https://docs.docker.com/engine/security/rootless/
  - type: systemd
    system:
      disable:
      # Disable rootful Docker
      - docker.service
      - docker.socket
  - type: script
    snippets:
      # Disable rootful Docker
      - "rm /var/run/docker.sock || true"
