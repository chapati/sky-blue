# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:
  - type: dnf
    repos:
      files:
        - ttps://download.sublimetext.com/rpm/stable/x86_64/sublime-text.repo
    install:
      packages:
        - sublime-merge